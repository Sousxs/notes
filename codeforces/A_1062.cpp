// @Dubega(Sousxs)
#include <bits/stdc++.h>
using namespace std;
#define int long long
void solve(int n){
	while(n--);
	int a, b, c, d;
	cin >> a >> b >> c >> d;
	if(a==b && a==b && a==c && a ==d)
		cout << "YES\n";
	else
		cout << "NO\n";
}
signed main(){
	int n; cin >> n;
    while(n--) solve(n);
    return 0;
}
