#include <bits/stdc++.h>
using namespace std;
#define int long long
#define endl '\n'
void solve(int n){
    int n; cin >> n;
    
}
signed main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int q; cin >> q;
    while(q--) solve(q);
    return 0;
}